using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This will handle all stats for creatures, items, plants, spells, and attacks
/// </summary>

/*public enum eCreatureType { player, enemy, npc }
public enum ePlantType { crop, forage, tree}
public enum eSpellType { fire, water, earth, air }
public enum eCropType { day, blood, night }
public enum eWeaknessType { fire, water, earth, air, none }
public enum eStrengthType { fire, water, earth, air, none }
public enum eGrowthStage { seed, sprout, mature, harvestable }
public enum eSpellUpgradeLevel { basic, advanced, expert }
public enum eItemType { consumable, quest, material }
public enum eStatusEffect { burn, slip, push, stun, none }

[CreateAssetMenu(fileName = "New Stats", menuName = "Create Stats/Create Stats")]

public class StatsDef : ScriptableObject
{
    // Common stats for everything
    public string description;

    // Common stats for creatures and the player
    public eWeaknessType weaknessType;
    public eCreatureType creatureType;
    public eStrengthType strengthType;
    public string creatureName;
    public int maxHealth;
    public List<string> enemyDrops;

    // Common stats for items
    public eItemType itemType;
    public int itemValue;
    public int itemStackSize;

    // Common stats for plants
    public ePlantType plantType;
    public eCropType cropType;
    public int growthTime; // TODO define time unit (seconds, minutes, hours)
    public eGrowthStage growthStage;
    public int bloodCropKillsNeeded; // number of kills needed to grow blood crops
    public int plantYield; // number of items produced when harvested
    public int seedYield; // number of seeds produced when harvested if plantable

    // Common stats for spells
    public eSpellType spellType;
    public int spellSize; // area of effect size
    public eSpellUpgradeLevel spellUpgradeLevel;
    public int spellDuration; // duration of spell effect
    public bool isAttackSpell;
    public int castDelay; // delay before spell is cast

    // Common stats for non-attack spells
    // TODO determine any stats needed for non-attack spells

    // Common stats for attacks / attack spells
    public eStatusEffect statusEffect;
    public int attackPower;
    public bool appliesStatusEffect;
    public int statusDuration;
    public float statusChance; // percentage chance to apply status effect
    public int attackRange;
    public int kockbackForce;

    // Common stats for potions
    public List<string> potionIngredients;

}*/
