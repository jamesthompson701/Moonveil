using UnityEngine;

/// <summary>
/// Boss shield manager:
/// - enable/disable shield and arena renderer/collider
/// - manage weakpoints (activate up to N random weakpoints)
/// - disable shield when all active weakpoints are destroyed
/// - public API for boss (health thresholds, death)
/// </summary>
public class BossShield : MonoBehaviour
{
    
}
